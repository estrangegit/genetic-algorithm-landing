# Genetic algorithm implementation for landing simulation

## Installation

In order to run npm scripts defined in package.json file, please provide a **.env** file with the following properties:

- SERVER_PORT
- SERVER_HOST_NAME
